# intelligent-qa-service/requirements.txt - MINIMAL dependencies for t2.large
fastapi==0.109.0
uvicorn==0.27.0
httpx==0.26.0
pydantic==2.5.3
mysql-connector-python==8.2.0
python-multipart==0.0.6

# Removed heavy dependencies:
# - numpy (not needed for simple text processing)
# - scikit-learn (not needed for basic similarity)
# - nltk (replaced with simple text processing)
# - regex (using built-in re module)
