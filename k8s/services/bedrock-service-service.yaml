apiVersion: v1
kind: Service
metadata:
  name: bedrock-service
  namespace: bedrock-chat-v2
  labels:
    app: bedrock-service
spec:
  ports:
  - port: 9000
    targetPort: 9000
    name: http
  selector:
    app: bedrock-service
  type: ClusterIP
