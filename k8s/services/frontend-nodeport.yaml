apiVersion: v1
kind: Service
metadata:
  name: frontend-nodeport
  namespace: bedrock-chat-v2
  labels:
    app: frontend
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30000
    name: http
  selector:
    app: frontend
