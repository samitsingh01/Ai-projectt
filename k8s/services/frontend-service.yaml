apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: bedrock-chat
  labels:
    app: frontend
spec:
  ports:
  - port: 3000
    targetPort: 3000
    name: http
  selector:
    app: frontend
  type: ClusterIP
