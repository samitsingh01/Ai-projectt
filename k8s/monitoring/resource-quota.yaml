apiVersion: v1
kind: ResourceQuota
metadata:
  name: bedrock-chat-quota
  namespace: bedrock-chat
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 4Gi
    limits.cpu: "4"
    limits.memory: 8Gi
    persistentvolumeclaims: "5"
